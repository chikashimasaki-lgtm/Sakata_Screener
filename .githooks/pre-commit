#!/usr/bin/env bash
# 個人情報・パスワードのコミットを止める。
# 本体は gas-shared/tools/scan-secrets.sh（ステージ済みの追加行のみを検査）。
#
# gas-shared が見つからないときは **黙って通す**。
# フックが原因でコミットできなくなるほうが害が大きく、CI 側でも同じ検査をしているため。
SCAN="$HOME/projects/gas-shared/tools/scan-secrets.sh"
[ -x "$SCAN" ] || exit 0
exec "$SCAN" --quiet
