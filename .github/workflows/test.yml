name: test

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 共有モジュール（gas-shared）を取得
        uses: actions/checkout@v4
        with:
          repository: chikashimasaki-lgtm/gas-shared
          path: .gas-shared
          # gas-shared は private。読み取り専用のデプロイキー（gas-shared 1個に限定）で clone する。
          # PAT はアカウント全体に効いてしまうため使わない。
          ssh-key: ${{ secrets.GAS_SHARED_KEY }}
      - name: src/ の共有モジュール symlink を実体化
        run: |
          for l in src/*.js; do
            [ -L "$l" ] || continue
            name=$(basename "$l")
            if [ -f ".gas-shared/modules/$name" ]; then
              rm "$l"
              cp ".gas-shared/modules/$name" "src/$name"
              echo "materialized: $name"
            fi
          done
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: 個人情報・パスワードの混入検査
        run: .gas-shared/tools/scan-secrets.sh --all
      - name: テスト（node tests/verify.js）
        run: node tests/verify.js
